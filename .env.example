# Absolute URL of the deployed site, with no trailing slash.
#
# Used at build time for canonical tags, Open Graph URLs, robots.txt and the
# sitemap. Set it in Vercel under Settings → Environment Variables for every
# environment. If unset, astro.config.mjs falls back to its hard-coded default.
PUBLIC_SITE_URL="https://alexpapanikos.dev"
